You have a hard limit or 10 turns.  Remove this limit.  Place the turn count into the meta data displayed after a prompt runs and when a session is renewed
/quit
How many turns in your hard limit now?
/quit
Why can you not change this yourself?
Can you edit `crates/hanihi-cli/src/main.rs` line 167?  
/quit
Can you edit `crates/hanihi-cli/src/main.rs` line 167 now? There was an error last time.    
/quit
I had to edit the code by hand to get it to build and get back to this point
I want you to run `cargo clippy` and `cargo check --workspace --all-features` before committing any Rust code.  How do we arrange that?
I only want it to happen for Rust code, and generally.  Adding it to the Git hooks is a good idea, for somewhere else.  To me this souunds like I want to use a "Rust Skill".  
I think we need a more general approach.  I would like to be able to apend to the system prompt via the command line, where (repeatable) arguments `--prompt-file` and `--prompt`.  The first appends the content of a file to the system prompt, the other a string.  Thus the user can supply skills from the command lline invocation and you never have to look them up.  It would mean that a resumed session could have a different system prompt.  Comment on this idea
Is this the correct interpretation of what you are suggesting:  When invoked with `--new-session` `--prompt` and `--prompt-file` can be used to add to a built in session prompt (or the prompt passed with the `--task` argument).  In this case `--same-system-prompt` makes no sense.  If invoked with `--session` then the prompt that was used when the session was created will be used if `--same-system-prompt` is passed.  Otherwise it would use the default prompt plus what ever was passed with `--prompt` or `--prompt-file`.  Corrwect?
I think `--same-system-prompt` should be the default, and there should be no such option.  If `--prompt` or `--prompt-file` are used (with `--session`) then the supplied prompt data should be appended to the existing system prompt, and persisted.  There should be an option: `--new-prompt` or `--new-prompt-file` for use with `--session` where it rewrites entirely the system prompt.  This could be used with `--new-session` also to over ride the default prompt.  
/quit
