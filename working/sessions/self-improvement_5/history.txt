The apply_patch` builtin is failing, almost always.  Tell me why
What can be done about it?
YesDo the highest value lowest risk option.  Do not use apply_patch
Before continuing allow non mutating uses of `hash-object` git subcommand.
Do not use `apply_patch` 
Do not use `apply_patch`. Allow non mutating uses of `hash-object` git subcommand. Do not use `apply_patch` 
Did you do it?
Confirmed
did `write_file` fail?
yes
Did you make the change?
/quit
Did you make the change?
You start the tool `write_file` but it does not complete
Did you make the change?
/quit
