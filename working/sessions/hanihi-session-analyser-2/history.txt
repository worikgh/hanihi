The binary `analyse` is going to have to read the JSON session files.  Start by making it take a session name on the command ine, and output for each entry from the associated `events.jsonl`  the `kind` and time stamp (`ts`)
The task is complete.  Update `git`
continue without `cargo run`
Make the binary `analyse`  take a session name on the command ine, and output for each entry from the associated `events.jsonl`  the `kind` and time stamp (`ts`)
/session
summerise this session so far
/file /tmp/prompt.md
Why does `run_streaming` skip `llm_prompt`/`llm_response` entries and ignores `LogWriter` errors?
/file /tmp/prompt.md
/session
Continue without `apply_patch`
/session
Incorporate `clap` into `analyse`.  Make the default action (no arguments) to list all available sessions.  On argument for now: `--session <session name>` does the same thing as the default action now.  
/session
/file /tmp/prompt.md
Implement option 2
/session
/quit
