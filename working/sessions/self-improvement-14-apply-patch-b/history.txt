Add a `dbg!()` macro to the first line of the function `builtin_get_time` in `tool.rs` of the `hanihi-core` crate
Tidy up your hanihi git repository.  There are seven modified files.  Commit them with sensible messages.  Do not push.
/session
What `run_command` just failed?
When a tool like that fails I want you to report and continue.  Only stop when there is no way to continue
What should be added to the system prompt to ensure that happens?
Yes
Where is the default system prompt
/quit
