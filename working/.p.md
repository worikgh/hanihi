In the README.md there are  **Undocumented eval assertions** — the Eval runner section lists nine assertion types; `build_succeeds`, `tests_pass`, `clippy_clean`, and  `no_diff` are implemented but undeclared.

Fix that

