The struct `LlmPromptData` has a member `pub messages: serde_json::Value`

Confirm that it is  a JSON representation of:

* The system prompt 
* `rig::completion::Message` 
* User input 

If it is, outline what sould be involved in creating a type `enum ContextMessages` (suggest a better name if needed) that combines all the types of data in `LlmPromptData.messages` to carry the data around the programme.

The point being to delay converting the data to JSON until it must leave the programme (it is written to the log or sent to the LLM)

The signature of `messgaes_for_log` would become:`fn messages_for_log(&str, &[Message], &[Message], &str,) -> Result<Vec<ContextMessage>, AgentError>`

I want a hgh level analysis
