{
  "command": "git diff",
  "duration_ms": 14,
  "exit_code": 0,
  "stderr": "",
  "stdout": "diff --git a/README.md b/README.md\nindex 198712c..c91911a 100644\n--- a/README.md\n+++ b/README.md\n@@ -25,6 +25,8 @@ crates/\n ├── hanihi-cli/         # binary: clap CLI + reedline REPL + --once mode\n ├── hanihi-eval/        # binary: eval runner — run test cases against a live\n │                       #   LLM and check assertions against the session log\n+├── hanihi-session/     # binary: read-only session reporting — print /session\n+│                       #   summaries from session.json + events.jsonl (no model)\n └── mcp-echo-server/    # binary: minimal MCP stdio server (demo, one\n                         #   `mcp_echo` tool)\n evals/\n@@ -266,9 +268,6 @@ cargo publish -p hanihi-core --dry-run  # full verification, no upload\n   `symbols` module (definitions, signatures, kinds, line numbers); a\n   `symbols(path)` tool or startup index under `working/`; reference-finding\n   to support multi-file refactoring.\n-- **Multi-file refactoring** — agent emits a plan applied as one multi-file\n-  unified diff via `apply_patch`, verified by workflow gates + before/after\n-  evals.\n - **Background workers** — in-process task layer (durable task state in the\n   event log, `read_task` tool, file-change watcher). The `self-improve.sh`\n   driver script is the minimal external version; do that first.\n@@ -276,3 +275,35 @@ cargo publish -p hanihi-core --dry-run  # full verification, no upload\n   pass/fail + token usage between runs (stretch goal from plan 005).\n - `add_ignore` tool / `--regenerate-ignore` — grow `.ignore` from within the\n   agent.\n+- **README drift** — this document no longer matches the code in several\n+  places and needs correction (see the audit in the next few bullets):\n+  - **Clippy is not clean** — the README claims\n+    `cargo clippy --workspace --all-targets -- -D warnings` passes. It does\n+    not: `tool.rs` has an unused `crate::debug` import, and `agent.rs`\n+    `run_streaming_loop` takes 8 arguments (`clippy::too_many_arguments`).\n+    Fix the code or drop the claim.\n+  - **Crate layout omits `hanihi-session`** — the Layout tree and feature\n+    lists only show `hanihi-core`, `hanihi-cli`, `hanihi-eval`, and\n+    `mcp-echo-server`; `hanihi-session` is a workspace member and\n+    undocumented.\n+  - **Stale test count and version** — Status claims \"39 unit tests\".\n+    `cargo test --workspace -- --list` reports 82 (72 core + 2 cli + 1 rig\n+    integration + 7 session). The version referenced is 0.2.0; the workspace\n+    is 0.3.0.\n+  - **`--max-turns` defaults wrong** — the config table says 10 (50 in task\n+    mode). The CLI (`main.rs`) sets both `DEFAULT_MAX_TURNS` and\n+    `TASK_MAX_TURNS` to 1000.\n+  - **Undocumented CLI flags** — `--prompt`, `--prompt-file`, and\n+    `--new-prompt` exist in `main.rs` but are absent from the config and REPL\n+    docs.\n+  - **Undocumented eval assertions** — the Eval runner section lists nine\n+    assertion types; `build_succeeds`, `tests_pass`, `clippy_clean`, and\n+    `no_diff` are implemented but undeclared.\n+  - **`run_command` allowlist mismatch** — the tool description and README\n+    advertise read-only `cargo`/`git` verbs, but `check_command_argv` also\n+    permits `git add`, `git commit`, ~90 non-mutating Unix commands, wider\n+    git inspection verbs, and `find`. This contradicts the \"write tools are\n+    opt-in\" security boundary. Align the description with the code or narrow\n+    the allowlist.\n+  - **Duplicated TODO** — the `add_ignore tool / --regenerate-ignore` bullet\n+    appears twice in this section. Keep one.\ndiff --git a/prompts/code_generation.md b/prompts/code_generation.md\nindex c15b0d1..b5d22f8 100644\n--- a/prompts/code_generation.md\n+++ b/prompts/code_generation.md\n@@ -44,7 +44,7 @@ Rule N: Sign all commit messages as: \"Hānihi\"\n \n ## Patch tooling (apply_patch / write_file)\n \n-- `apply_patch` accepts only unified diffs:                                                                                                             `--- a/<path>` / `+++ b/<path>` headers and `@@ -l,c +l,c @@` hunks.\n+- `apply_patch` accepts only unified diffs:`--- a/<path>` / `+++ b/<path>` headers and `@@ -l,c +l,c @@` hunks.\n   Never emit `*** Begin Patch`, `*** Update File:`, or any other patch format.\n - Before producing a diff, read the current contents of every file it\n   touches with `read_file`, or generate it with `git diff`. Copy all\n",
  "ts": "2026-09-07T00:39:51.251056710+00:00"
}